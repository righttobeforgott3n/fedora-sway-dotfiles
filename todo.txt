- [x] battery consumption and power management.
- [x] modify the waybar style to appear as a unique block and not three separated tab.
- [x] use FiraCode as font.
- [x] customize swaylock with a Catppuccin Frappe based and minimalistic style.
- [x] modify the waybar order components (to decide about the composition) and put on the left corner the active application settings (as MacOS).
- [x] start fnott (under variable notification_daemon) and ProtonVPN app at startup.
- [x] install a screenshotter app.
- [x] think to a better main waybar components composition.
- [x] remove the window title on the waybar left side.
- [x] the current workspace must be evident with some effects (coherent with the system aestethic).
- [x] the install.sh script must backup the current and in use (so the folders under $HOME/.config) configuration before applying the new one in way to recover the system in case of bugs.
- [x] customize the rofi aestefc-list | grep -i hackthic with a look coherent with the system.
- [x] bug: every time a worksapce tab is clicked the mouse is positionated to the new workspace centre.
- [x] bug: the following icons are broken: the connection icon.
- [] enh: improve rofi menu to launch command as I was executing them from the shell (is it even possible?).
- [x] enh: incude the gtk installed them within the installer script.
- [x] enh: add zsh and plugins.
- [x] enh: sync. the fonts import and installation within the installer script.
- [] enh: add workspace-specific window rules and auto-assignment (e.g., browser on ws1, terminals on ws2, etc.).
- [] enh: configure system-wide notifications theming for fnott to match the color palette.
- [] enh: add keyboard shortcuts for common tasks (screenshot regions, color picker, clipboard manager).
- [] enh: implement window gaps and borders with aesthetic styling.
- [] enh: add workspace auto-naming based on active applications (dynamic workspace names).
- [] enh: configure autotiling script/plugin for automatic window layout management.
- [] enh: add brightness and volume OSD (on-screen display) notifications.
- [] enh: implement scratchpad terminal (dropdown terminal accessible via hotkey).
- [] enh: add system resource monitor module to waybar (CPU, RAM, network speed).
- [] enh: configure keybindings for window snapping (quarter tiling, thirds, etc.).
- [] enh: add clipboard history manager integration (e.g., clipman).
- [] enh: implement focus follows mouse with configurable delay.
- [] enh: add playerctl integration to waybar for media control.
- [] enh: configure inhibit idle when watching videos or during presentations.